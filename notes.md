# Notes

## Prediction

I predict that the more powerful model will produce a noticeably better summary of the code. It should explain the logic and changes in more detail.

## Claude's Summary

The `getDiscount` function takes a price and a discount code. If the code is `SAVE10`, it applies a 10% discount by multiplying the price by 0.9. If the code is `SAVE20`, it applies a 20% discount by multiplying the price by 0.8. If another code is provided, the function does not return a discounted price.
